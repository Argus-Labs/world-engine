---

- name: Copy binary
  copy:
    src: "{{BINARY}}"
    dest: /usr/bin/argusd
    mode: 0755
  notify: restart argusd

- name: Copy new genesis.json file, if available
  when: "GENESISFILE is defined and GENESISFILE != ''" 
  copy:
    src: "{{GENESISFILE}}"
    dest: /home/argusd/.argus/config/genesis.json
  notify: restart argusd

- name: Download genesis.json URL, if available
  when: "GENESISURL is defined and GENESISURL != ''"
  get_url:
    url: "{{GENESISURL}}"
    dest: /home/argusd/.argus/config/genesis.json
    force: yes
  notify: restart argusd

- name: Reset network
  when: UNSAFE_RESET_ALL | default(false) | bool
  command: "sudo -u argusd argusd unsafe-reset-all"
  notify: restart argusd
