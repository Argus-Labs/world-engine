apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: argus-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/argus-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/argus-default: "true"
