---
title: World Forge
---

World Forge has been retired. Its managed deployments and region presets are no longer available.

Configure your gateway URLs and region IDs explicitly using the
[SDK configuration guide](/sdk/javascript/guides/configuration). Region IDs must match the
regions configured on your gateways and shards. The GCP deployment uses `us-west1`.
