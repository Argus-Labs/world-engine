---
title: 'Deployment'
---

World Forge has been retired. The former `world deploy`, `world promote`, `world status`,
`world logs`, `world reset`, and `world destroy` deployment workflow is no longer available.

Deploy gateways and shards through your infrastructure workflow. Configure the same region ID
on the gateway, shards, and clients. The GCP deployment uses `us-west1`.
