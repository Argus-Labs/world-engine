# Reads

This API reference will help you understand how to use Reads and configuration options available for Reads.

```go
package reads

import "pkg.world.dev/world-engine/cardinal"

type PlanetInfoRequest struct {
	TargetID uint64
}

type PlanetInfoReply struct {
	Owner   string
	Level   uint8
	Energy  uint64
	Cap     uint64
	Silver  uint64
}

var PlanetInfo = cardinal.NewReadType[PlanetInfoRequest, PlanetInfoReply](
	"planet-info",
	func(w *cardinal.World, req PlanetInfoRequest) (PlanetInfoReply, error) {
		// do some query stuff...
		reply := doSomeQueryThing(req)
		return reply, nil
	},
)
```

## NewReadType

`NewReadType` creates a new `ReadType`. `ReadType`s wrap around a request and reply type. The request specifies the data needed for the read request. The reply type represents the data requested. Reads also require a function that implements the read.

```go
func NewReadType[Request any, Reply any](
	name string,
	handler func(world *World, req Request) (Reply, error),
	opts ...func() func(readType *ReadType[Request, Reply]),
) *ReadType[Request, Reply]
```

### Type Parameters

| Type Parameter | Description                                     |
|----------------|-------------------------------------------------|
| Request        | The type parameter representing the request type. |
| Reply          | The type parameter representing the reply type.   |

### Parameters

| Parameter   | Type                                          | Description                                                                                        |
|-------------|-----------------------------------------------|----------------------------------------------------------------------------------------------------|
| name        | string                                        | The name of the new read type.                                                                    |
| handler     | func(world *World, req Request) (Reply, error) | The handler function for processing requests and generating a reply.                            |
| opts        | variadic functions of func() func(*ReadType[Request, Reply]) | Optional functional options that configure the properties of the read type.  |


### Return Value

| Return Type             | Description                                                             |
|-------------------------|-------------------------------------------------------------------------|
| *ReadType[Request, Reply] | A pointer to a new instance of ReadType[Request, Reply].                |

## Options

### WithReadEVMSupport

The `WithReadEVMSupport` enables this read to be sent from EVM smart contracts. Note: the option requires the type parameters again. This is needed so option can generate the concrete EVM equivalent types for the request and reply of the read.


import { Callout } from 'nextra/components'

import Link from 'next/link'

<Callout type={"warning"}>
    Not all Go types are supported when using `WithReadEVMSupport`. <Link href="/Cardinal/API-Reference/evm-support">Read More</Link>
</Callout>

```go
func WithReadEVMSupport[Request, Reply any]() func(transactionType *ReadType[Request, Reply])
```

